<UserControl x:Class="sifimo.Presentacion"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:sifimo"
             mc:Ignorable="d" 
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock VerticalAlignment="Bottom" Foreground="#3D8D7A" HorizontalAlignment="Center" FontSize="24" FontWeight="Bold" Text="Simulacion de las consecuencias de Lorentz."/>
        <Border Padding="8" VerticalAlignment="Top" HorizontalAlignment="Center" Grid.Row="1" Margin="10" CornerRadius="10" Background="#3D8D7A" >
            <Button Click="Button_Click" Background="Transparent"  Foreground="#FBFFE4" BorderThickness="0" Padding="7" FontWeight="Bold">
                Empezar
            </Button>
        </Border>
    </Grid>
</UserControl>
